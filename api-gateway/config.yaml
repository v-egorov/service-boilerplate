app:
  name: "api-gateway"
  version: "1.0.0"
  environment: "development"

logging:
  level: "debug"
  format: "json"
  output: "stdout"

server:
  host: "0.0.0.0"
  port: 8080

monitoring:
  health_check_timeout: 5
  status_cache_duration: 30
  enable_detailed_metrics: true