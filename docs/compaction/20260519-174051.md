## Goal
- Refactor permission architecture: unify naming conventions, fix contradictory role/permission matrices across documentation, and document infrastructure decisions (cache disabled) before implementing scoped variant migrations

## Constraints & Preferences
- Build mode enabled (air hot-reload for services)
- Scoped variants (`:own`, `:all`) required for permissions; unscoped variants must be removed from all docs
- Multi-role union rule: less restrictive wins across roles; within-role specificity enforced (Rule A — one scope per role/action pair)
- Cache disabled due to cross-container invalidation impossibility — in-memory LRU cache exists but not wired at startup

## Progress
### Done
- **Phase 1 Error Standardization:** All services complete (commits 7ef7606, 627092b, e55b2d5)
- **Phase 2 Success Enhancement:** All services complete (commits 6f359af, b034afa, 8c9f5bd, 7f1c0b7)
- **Auth-service UserLoginResponse fix:** Added Data wrapper (commit e82e6bd)
- **test-auth-flow.sh:** Now working correctly
- **Documentation:** `api-response-standardization-plan.md` updated, `inter-service-communication.md` created (commit 3ac8cc3)
- **test-rbac-relationships.sh format fix:** Changed `.public_id` → `.data.public_id` on lines 175 & 219. RL-1 passes now.
- **RL-3 root cause identified:** `user` role lacks `relationships:read` permission (only admin/object-type-admin have it) — rollback applied by user, will fix during Phase 1 migrations
- **Long-term permissions architecture designed:** Scoped variants, new roles (`relationship-admin`, `relationship-viewer`), multi-role union rule, within-role specificity constraint
- **Middleware audit completed:** Found cache invalidation gap (never called outside tests, only TTL-based eviction of 60s) and relationship handler has zero ownership checks
- **Compaction infrastructure added to AGENTS.md** — compaction summaries now written to `docs/compaction/YYYYMMDD-HHmmss.md` automatically (commit c19dbdf)
- **Documentation rewritten for consistency:** All role/permission matrices updated to show exactly one scope level per role/action pair; cross-file contradictions resolved (commits b164b3e, e223109, c19dbdf, a50ee10)

### In Progress
- **Phase 0 of permissions implementation plan:** Documentation audit complete; middleware enforcement audit done but not yet implemented in code

### Blocked
- None

## Key Decisions
- **Remove unscoped permission variants** (`relationships:create`, etc.) → replace with scoped-only (`create:own`, `create:all`)
- **Two-rule resolution model:** (1) Within-role: most specific wins; (2) Across-roles: union, broadest (`:all`) overrides narrower (`:own`)
- **New roles introduced:** `relationship-admin` (full CRUD on rels), `relationship-viewer` (read-only audit/discovery)
- **object-type-admin role scoped strictly to object-types + objects** → no relationship permissions; full CRUD on object types registry, read-only on data instances
- **Assignment constraint enforced:** Prevent both `type:action:own` AND `type:action:all` on same role via POST/PATCH/PUT validation
- **Cache disabled for now:** PermissionCache exists but not wired at startup due to cross-container invalidation impossibility (in-memory LRU with 60s TTL causes stale data across replicas)
- **Cross-permission info leakage accepted:** `relationships:read:all` exposing object IDs is acceptable; handled by deliberate permission/role assignment
- **Create action has no scoped variants on objects/types** — flat `object-types:create`, `objects:create`; only relationships get `create:own`/`:all` since creating a relationship requires ownership of pre-existing endpoint objects

## Next Steps
1. Execute Phase 1 migrations (scoped variants in `000008_add_relationships_permissions.up.sql`, new roles via `000010_add_relationship_roles.up.sql`, updated assignments in `000009_assign_relationships_permissions.up.sql`) across development, staging, production environments
2. Implement middleware enhancements for scoped variant enforcement & multi-role union logic (Phase 2) — add cache invalidation hooks to mutation endpoints first
3. Add ownership checks to relationship handler (`relationship_handler.go` currently has zero protection on create/update/delete)
4. Add/update RBAC tests (Phase 3)

## Critical Context
- **RL-1 status:** Fixed and passing after correcting jq path from `.public_id` to `.data.public_id`
- **RL-3 expectation clarified:** `user` role gets only `read:own` for relationships → returns 200 with filtered list containing only relationships where user owns at least one endpoint; no `read:all` granted in current plan
- **Documentation consistency confirmed:** Cross-file contradictions fully resolved; both architecture and implementation plan matrices now align with Rule A (one scope per role/action pair) and flat `create` semantics for object types/objects
- **Permission middleware currently checks flat names** → needs update to resolve scoped variants and verify ownership for `:own` types
- **Graph updated:** 1856 nodes, 5118 edges, 33 communities (commit 61ce2e1)
- **Cache invalidation gap confirmed:** AuthService struct stores `cache(cache.PermissionCache)` field; AuthService has both constructors (`NewAuthService()` and `NewAuthServiceWithCache()`); main.go uses WithCache variant; however no handler endpoint calls Invalidate() after mutations — only TTL eviction at 60s

## Relevant Files
- `/home/vergov/ai/service-boilerplate/docs/object-type-permissions-implementation-plan.md`: Fixed Step 3.1 RL-3 expectation to reflect `user` role gets filtered results via `read:own`; corrected `update:only` typo to `update:own`
- `/home/vergov/ai/service-boilerplate/docs/object-type-permissions-architecture.md`: Aligned Object Types Permissions table with flat `create` semantics; added note clarifying create action uses flat names on registry; verified zero Rule A violations across both docs
- `scripts/test-rbac-relationships.sh`: Fixed format assertions on lines 175 & 219; RL-3 needs Phase 1 migration fix (grant scoped permissions to user role)
- `services/auth-service/migrations/development/000009_assign_relationships_permissions.up.sql`: Needs update to grant scoped permissions to user role (rollback applied by user; will fix during Phase 1)
- `services/auth-service/migrations/development/000008_add_relationships_permissions.up.sql`: Will need expansion for scoped variants: `read:own`, `read:all`, `create:own`, `update:own`, etc. across all three environments (dev/staging/prod)
- `AGENTS.md`: Added Compaction section with rules for automatic compaction file generation to `docs/compaction/`